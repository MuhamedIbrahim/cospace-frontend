import styled from "styled-components";

const ProfileBookingsStyle = styled.div`
  max-width: 500px;
  margin: auto;
  .no_bookings {
    margin: 0;
    color: var(--lightGrey6);
  }
`;

export default ProfileBookingsStyle;
